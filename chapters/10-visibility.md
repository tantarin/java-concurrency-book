# Почему изменения нужно «увидеть»

> Visibility

← [wait(), notify() и notifyAll()](./09-wait-notify.md) · [Зачем придумали Java Memory Model](./11-java-memory-model.md) →

До сих пор речь шла об **atomicity (атомарности)**: действия не должны вклиниваться друг в друга. Но есть отдельная проблема — **visibility (видимость изменений между потоками)**.

JIT и процессор оптимизируют выполнение. Значение может находиться в регистре или кеше, а некоторые независимые инструкции могут быть переупорядочены. Если один поток записал значение, без специальных гарантий другой не обязан немедленно увидеть именно его.

```java
Thread 1: count = 10

Thread 2: какое значение ему разрешено увидеть?
```

Полностью запретить оптимизации было бы слишком дорого. Вместо этого Java определяет действия синхронизации, через которые устанавливается необходимый порядок.

> Atomicity (атомарность) отвечает «могут ли действия перемешаться?», а visibility (видимость) — «обязан ли другой поток увидеть результат?»

## В живом проекте

- [`volatile`-состояние загрузки](https://github.com/tantarin/java-concurrency-lab/blob/11c72fc3beda43fd3e744c2eedbf6e60bc93a9f6/downloader/src/main/java/io/github/tantarin/downloader/Download.java#L7-L14)

## Дальше

Единые правила должны работать на любых JVM и процессорах. Именно для этого существует Java Memory Model.

---

← [wait(), notify() и notifyAll()](./09-wait-notify.md) · [Зачем придумали Java Memory Model](./11-java-memory-model.md) →
